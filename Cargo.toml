[package]
name = "macros"
version = "0.1.0"
edition = "2021"
authors = ["Tecnocrat <your-email@example.com>"]
description = "Rust macro playground with automation and logging"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"