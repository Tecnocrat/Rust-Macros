name: Generate Workspace Index and Manifest

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install flask

      - name: Generate index and manifest
        run: |
          python main.py
          python update_metadata.py

      - name: Set up Git user
        run: |
          git config --global user.name "tecnocrat"
          git config --global user.email "jesussard@gmail.com"

      - name: Commit and push updated metadata
        run: |
          git add workspace_index.json exec_times.log
          git commit -m "Auto-update workspace index and manifest [CI skip]" || echo "No changes to commit"
          git push